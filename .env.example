# Copy to .env and fill in values
# VITE_API_BASE_URL=http://localhost:8000
# Set to "true" to use local case JSON data instead of calling the real API.
# In demo mode, clicking a case loads its snapshot and returns the mock prediction locally.
VITE_DEMO_MODE=false
VITE_API_BASE_URL=https://yeiaimmbcg.execute-api.us-east-2.amazonaws.com/prod
